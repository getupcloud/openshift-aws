#!/bin/bash -e

source config/aws.conf
source lib/util

BUCKET_NAME="`var_value BUCKET_NAME`"
if [ ${BUCKET_NAME} ]; then
    lib/request delete $AWS_STORE_BASE_URL/s3/$BUCKET_NAME/ --data "cartridge=getup-aws-s3-us-west-2" || true
fi
